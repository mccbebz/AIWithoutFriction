document.getElementById('leadForm')?.addEventListener('submit', function(){
  // Static fallback: mailto opens the visitor's email app. For production, replace the form action
  // with Formspree, Google Forms endpoint, or a server-side handler on AWS.
});
